package br.com.stfturismo.controller;

import java.util.logging.Logger;

import javax.enterprise.inject.Model;

@Model
public class BaseListController extends BaseController {

	private static final Logger log = Logger.getLogger(BaseListController.class.getSimpleName()); 
	
	
}
